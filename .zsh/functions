gh-clone() {
	git clone "git@github.com:$1"
}

java-exec() {
	javac $1
	java "${1//.java}" $2
	rm ./*.class
}

nix-shell-active() {
	local LEN=${#shellHook}

	if [ $LEN -gt 0 ]; then
		return 0;
	else
		return 1;
	fi
}

